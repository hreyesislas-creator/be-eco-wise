import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Be Eco Wise | Rubber Mulch Installation",
    template: "%s | Be Eco Wise",
  },
  description:
    "Professional rubber mulch installation for playgrounds, schools, HOAs, parks, and residential landscapes.",
  icons: {
    icon: [{ url: "/images/be-eco-wise-logo.png", type: "image/png" }],
    apple: "/images/be-eco-wise-logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
